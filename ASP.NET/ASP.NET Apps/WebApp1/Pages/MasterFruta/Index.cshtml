@page
@model WebApp1.Pages.MasterFruta.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.MasterFruta[0].Descripcion)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MasterFruta[0].Tienda_Id)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.MasterFruta) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Descripcion)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Tienda_Id)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Fruta_Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Fruta_Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Fruta_Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
